distance = float(input("Enter distance "))

fuel_efficency = float(input("enter fuel efficency "))

price_per_gallon = float(input("enter enter price per gallon "))

price =(distance*price_per_gallon)/fuel_efficency

print("cost of driving is $ "+str(price))
